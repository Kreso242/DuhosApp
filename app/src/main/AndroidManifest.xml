<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.duhos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/duhos"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/duhos"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Songs"></activity>
        <activity android:name=".Questions" />
        <activity android:name=".Calendar" />
        <activity android:name=".Multimedia" />
        <activity android:name=".Molitva" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>